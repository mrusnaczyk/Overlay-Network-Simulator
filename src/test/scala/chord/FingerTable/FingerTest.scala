package chord.FingerTable

import akka.actor.ActorSystem
import akka.testkit.{TestKit, TestProbe}
import org.scalatest.BeforeAndAfterAll
import org.scalatest.funspec.{AnyFunSpec, FixtureAnyFunSpec}
import org.scalatest.funsuite.{AnyFunSuite, AnyFunSuiteLike}

class FingerTest {//extends TestKit(ActorSystem("TestSystem")) with AnyFunSuiteLike {
  /*test("getNode() should return the correct node") {
    val testProbe = TestProbe()
    val finger: Finger = new Finger(1, (1, 5), (1, testProbe.ref))
    assert(finger.getNode.get.equals((1, testProbe.ref)))
  }

  test("isNodeInInterval() should succeed when another node is in range") {
    val finger = new Finger(1, (1, 5), null)
    assert(finger.isNodeInInterval(2))
  }

  test("isNodeInInterval() should fail when nodeId is the same as end of range") {
    val finger = new Finger(1, (1, 5), null)
    assert(finger.isNodeInInterval(5) == false)
  }

  test("isNodeInInterval() should succeed when nodeId is the same as start of range") {
    val finger = new Finger(1, (1, 5), null)
    assert(finger.isNodeInInterval(1))
  }*/

}
